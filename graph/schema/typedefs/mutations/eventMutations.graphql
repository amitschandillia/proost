# Definitions: Input objects
input EventInput {
  title: String!,
  description: String!,
  price: Float!,
  date: String!
}

# Definitions: Mutations
type Mutation {
  createEvent(eventInput: EventInput!): Event
}
