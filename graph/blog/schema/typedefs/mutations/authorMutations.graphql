# Definitions: Input objects
input AuthorInput {
  firstName: String!,
  lastName: String!,
  email: String!,
  bio: String!,
}

# Definitions: Mutations
type Mutation {
  createAuthor(authorInput: AuthorInput!): Author
}
