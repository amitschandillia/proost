# Definitions: Input objects
input AuthorData {
  firstName: String!,
  lastName: String!,
  email: String!,
  bio: String!,
}

# Definitions: Mutations
type Mutation {
  createAuthor(authorData: AuthorData!): Author,
}
