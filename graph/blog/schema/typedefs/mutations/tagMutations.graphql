# Definitions: Input objects
input TagInfo {
  name: String!,
  description: String!,
}
input OldTagInfo {
  _id: ID!
  name: String!,
  description: String!,
}

# Definitions: Mutations
type Mutation {
  createTag(tagInfo: TagInfo!): Tag,
  updateTag(oldTagInfo: OldTagInfo!): Tag,
}
