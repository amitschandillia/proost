# Definitions: Types
type Post {
  _id: ID!,
  title: String!,
  content: String!,
  isPublished: Boolean!,
  # Milestones
  createdAt: DateTime!,
  updatedAt: DateTime!,
  publishedAt: DateTime,
  author: Author!,
  tags: [Tag],
  category: Category
}
