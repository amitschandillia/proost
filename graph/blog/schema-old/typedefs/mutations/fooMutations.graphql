# Definitions: Input objects
input FooInfo {
  title: String!,
}
input NewFooInfo {
  _id: ID!
  title: String!,
}

# Definitions: Mutations
type Mutation {
  createFoo(fooInfo: FooInfo!): Foo,
  updateFoo(newFooInfo: NewFooInfo!): Foo,
}
