query posts($slug: String!) {
  posts(limit: 1, where: { slug: $slug }) {
    _id
    title
    secondaryTitle
    readTime
    body
    banner {
      hash
      ext
    }
    author {
      _id
      username
      firstName
      lastName
    }
  }
}
