{
  "defaultConnection": "default",
  "connections": {
    "default": {
      "connector": "strapi-hook-mongoose",
      "settings": {
        "host": "${process.env.DATABASE_HOST || 'cluster0-mg1j6.mongodb.net'}",
        "port": "${process.env.DATABASE_PORT || 27017}",
        "srv": "${process.env.DATABASE_SRV || true}",
        "database": "${process.env.DATABASE_NAME || 'dbCMS2'}",
        "username": "${process.env.DATABASE_USERNAME || 'usrCMS2'}",
        "password": "${process.env.DATABASE_PASSWORD || 'T9MQjCVeoFPU5mrr'}",
        "ssl": "${process.env.DATABASE_SSL || true}"
      },
      "options": {
        "ssl": "${process.env.DATABASE_SSL || true}",
        "authenticationDatabase": "${process.env.DATABASE_AUTHENTICATION_DATABASE || 'admin'}"
      }
    }
  }
}
